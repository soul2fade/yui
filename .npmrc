; vite-react-ssg's published peer range tops out at Vite 7, but it builds fine
; against Vite 8. Allow the install to proceed so Netlify CI `npm install` /
; `npm ci` doesn't fail on the ERESOLVE peer conflict.
legacy-peer-deps=true
